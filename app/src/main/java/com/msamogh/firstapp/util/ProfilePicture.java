/*
package com.msamogh.firstapp.util;

import android.graphics.Color;

import com.msamogh.firstapp.R;

*/
/**
 * Created by Amogh on 7/4/15.
 *//*

public class ProfilePicture {

    public static int getColoredParentColor(String username) {
        if (username != null && !username.isEmpty()) {
            username = username.toUpperCase();
            char ch = username.charAt(0);
            if (ch % 7 == 0)
                return Color.parseColor("#ff4b9416");
            if (ch % 6 == 0)
                return Color.parseColor("#000000");
            if (ch % 5 == 0)
                return Color.parseColor("#ff1b4094");
            if (ch % 4 == 0)
                return Color.parseColor("#ffd16419");
            if (ch % 3 == 0)
                return Color.parseColor("#ff7b7e62");
            if (ch % 2 == 0)
                return Color.parseColor("#ffb93499");
            else
                return Color.parseColor("#ff1b4094");
        }
        return -1;
    }

    public static int getProfilePictureRes(String username) {
        if (username != null && !username.isEmpty()) {
            username = username.toUpperCase();
            char ch = username.charAt(0);
            if (ch % 7 == 0)
                return R.drawable.circle_green;
            if (ch % 6 == 0)
                return R.drawable.circle_black;
            if (ch % 5 == 0)
                return R.drawable.circle_blue;
            if (ch % 4 == 0)
                return R.drawable.circle_orange;
            if (ch % 3 == 0)
                return R.drawable.circle_date;
            if (ch % 2 == 0)
                return R.drawable.circle_pink;
            else
                return R.drawable.circle_blue;
        }
        return -1;
    }

}
*/
